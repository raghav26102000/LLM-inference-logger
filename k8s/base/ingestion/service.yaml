apiVersion: v1
kind: Service
metadata:
  name: ingestion-svc
  namespace: llm-logger
  labels:
    app: ingestion
spec:
  selector:
    app: ingestion
  ports:
    - name: http
      port: 4000
      targetPort: 4000
  type: ClusterIP
